pipeline:
  task1:
    range: 1..002
    description: '@desc'
    subPath: xxx/${index}*
    template:
      action1:
        action: print
        message: action 1 - message from ${index} ${subPath}
      action2:
        action: print
        message: action 2 - message from ${index}  ${subPath}
