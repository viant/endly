action: dsunit:init
datastore: $db
recreate: true
config:
  driverName: aerospike
  descriptor: "tcp([host]:3000)/[namespace]"
  ping: true
  parameters:
    dbname: $db
    namespace: test
    host: 127.0.0.1
    port: 3000
    dateFormat: yyyy-MM-dd hh:mm:ss
