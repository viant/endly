mapping:
  datastore: $db
  action: dsunit.mapping
  mappings:
    - URL: regression/$db/mapping.json
  post:
    tables: $Tables
setup:
  datastore: $db
  action: dsunit:prepare
  URL: regression/$db/data/
