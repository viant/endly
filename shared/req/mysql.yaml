name: service/mysql
tasks: $tasks
params:
  stopSystemMysql: true
  serviceTarget: $serviceTarget
  target: $target
  config: $Pwd(config/my.cnf)
  credentials: $mysqlCredentials
  name: $instance
  version: $mysqlVersion
  dump: $dump
